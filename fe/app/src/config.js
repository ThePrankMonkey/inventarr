const config = {
  BACKEND_URL: process.env.REACT_APP_BACKEND_URL,
  IMAGE_URL: process.env.REACT_APP_BACKEND_URL,
  MIN_LABEL_WIDTH_INCHES: 0,
  MAX_LABEL_WIDTH_INCHES: 2.4,
};

export default config;
